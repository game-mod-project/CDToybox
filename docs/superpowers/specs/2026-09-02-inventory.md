# 인벤토리 구조와 아이템 인스턴스

2026-09-02. 담금질·소켓이 반영된 장비를 게임 밖으로 빼내고 다시
넣으려면(export/import) 아이템 인스턴스를 읽어야 한다. 그 조사 기록.

## 왜 필요한가

지금 지급은 **담금질·소켓이 빈 장비**를 준다. 이 게임은 레벨이 없고
스탯이 장비에서 오므로, 캐릭터가 손댄 장비를 그대로 보관했다 꺼내는
것이 실질적인 기능이다.

## 인벤토리 레코드

서버 인벤토리 컴포넌트(`pa::ServerInventoryActorComponent`)가
컨테이너를 여럿 들고 있다. 각각 `{포인터, u32 사용, u32 용량}` 꼴이다.

```
컴포넌트 +0x78  레코드 배열   +0x80  사용 / 용량   (실측 37 / 41)
컴포넌트 +0x88  또 다른 컨테이너 (2 / 2)
컴포넌트 +0xB0  또 다른 컨테이너
```

**레코드 간격은 0xC8.**

```
레코드 +0x00  u64  인스턴스 ID      (18510, 18509, ... 연속으로 줄어든다)
       +0x08  u32  짧은 식별자      (5915, 202518, 71454 ...)
       +0x10  i64  개수
       +0x38  u64  ?
       +0x60  ptr  소켓 배열
       +0x68  u32 5 / u32 5         소켓 5칸
```

### 소켓

`+0x60` 이 가리키는 곳에 **6바이트 항목이 다섯 개** 있다.

```
FF FF 00 00 FF 02     {u32 소켓ID, u8 플래그, u8}
FF FF 00 00 FF 00
FF FF 00 00 FF 00
FF FF 00 00 FF 00
FF FF 00 00 FF 00
```

빈 칸은 `0xFFFF` / `0xFF` 다. `TrItemValue` 생성자가 `+0x40~0x5E` 에
깔아 두는 기본값과 같은 모양이다.

## 두 가지 헛짚음 (같은 실수를 반복하지 않도록)

**간격을 0x190 으로 잡았다.** 그러면 한 칸씩 건너뛰어 37개 중 19개만
보인다. 0x190 이 정확히 0xC8 의 두 배라 레코드가 그럴듯하게 정렬돼
보였다. 레코드 `+0xC8` 에 다음 레코드의 ID 가 있는 것을 보고 알았다.

**화면의 "99 / 130" 을 메모리에서 찾으려 했다.** 그 값은 UI 가 여러
컨테이너를 합산해 만든 것이라 메모리에 그대로 없다. u32 쌍
`(99, 130)` 을 검색했더니 유일한 후보가 연속된 정수 표였다.

## 식별 체계가 둘이다

인벤토리 레코드의 `+0x08` 은 아이템 표 키가 아니다.

```
loc item 1163042  ->  늑대의 한손검 (이름·설명 있음)
loc item 5915     ->  없음
loc item 202518   ->  없음
배열 범위에서 1163042 검색  ->  없음
레코드 전체에서 6810표 키 검색 -> 없음
```

### 변환 함수 (RVA 0x12F9570)

표 조회 함수를 후킹해 잡았다. 인벤토리 툴팁을 띄우면 걸린다.

```
스트림에서 u32 읽기        아이템 키 (1163042)
rcx = [0x6331358] + 0x68   아이템 표
call 0x31BBF0              조회
movzx eax, word ptr [rax]  레코드의 첫 u16
mov word ptr [rbx], ax     그게 짧은 식별자
```

**아이템 표 레코드의 첫 u16 이 인벤토리가 저장하는 짧은 식별자다.**

실측 로그:

```
표 조회: 키 1163042 표 0x31F65CB6DE8 부른 곳 모듈+0x12F95B1
표 조회: 키 1163044 ...
표 조회: 키 1163045 ...
표 조회: 키 1163046 ...
```

표 객체 `+0x08` 이 6810 - 아이템 개수와 맞는다.

### 남은 것

지금 아는 것은 정방향(아이템 키 -> 짧은 id)이다. export/import 에는
역방향이 필요하므로 표를 훑어 대응표를 만들어야 한다.

`[0x6331358]` 의 표는 우리가 쓰던 `ItemInfoManager` 와 레이아웃이
다르다(그쪽은 `+0x30` 개수, `+0x58` 레코드 포인터 배열). 이 표의
레코드 구조를 한 번 더 확인해야 한다 - `+0x10` 이 포인터인데 첫
8바이트가 vtable 이라 컨테이너 객체로 보인다.

## 도구

```
probe invlist <서버인벤토리컴포넌트주소>   컨테이너와 레코드를 나열
probe itemvalue [최대]                     힙에서 아이템 인스턴스 서명 검색
```

`invlist` 는 컴포넌트 앞부분을 훑어 `{포인터, 사용, 용량}` 꼴이면서
연속 두 레코드가 말이 되는 것을 컨테이너로 친다. 하나만 보면
개수가 300억인 것이 통과했다.

`+0x80` 의 값은 믿지 않고 말이 안 되는 레코드에서 멈춘다 - 실측에서
37 이라고 하는데 실제로는 그보다 적은 경우가 있었다.

모드 쪽 후킹(`table_probe_install`)은 찾는 키가 들어올 때만, 8회까지만
남긴다. 이 조회 함수는 초당 수천 번 불린다.
