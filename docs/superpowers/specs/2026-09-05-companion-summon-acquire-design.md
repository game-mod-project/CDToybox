# 동반자 소환·획득 기능 구상 (2026-09-05)

포획 목록(`2026-09-05-catchable-companions.md`)의 야생 탈것·반려동물을
오버레이에서 **불러내고(소환) 내 것으로 만드는(획득)** 기능의 설계.
실측·정적 분석으로 확인한 사실 위에 세웠고, 아직 안 해 본 것은 그렇게
표시했다.

## 0. 한 줄 요약

- **획득은 메시지 하나로 좁혀졌다.** 길들이기 성공이 보내는
  `TrocTrHireMercenaryToTargetReq`(ID 2338, 본문 `u32 대상 액터 핸들 + u8`)
  를 역직렬화 구동하면, 월드에 있는 야생 개체를 로데오·친밀도 없이
  등록할 가능성이 있다. 소켓 Phase 2 와 같은 인프라다.
- **소환(없는 개체를 만들기)은 여전히 어렵다.** 대신 "이미 스폰된 야생
  개체를 찾아가기"는 지금 인프라로 되고 위험이 없다. 순서를
  **목록 → 찾아가기 → 획득** 으로 잡고, 진짜 소환은 마지막에 둔다.

## 1. 확인된 사실 (설계의 근거)

### 1.1 엔진 모델

- 탈것·마차·펫·가축·물고기는 전부 `MercenaryInfo` 타입(21종)의 하위이고,
  캐릭터 표의 `_mercenaryInfo`(+0xBE, 행 번호)로 타입이 정해진다. 야생
  개체는 내부 이름 `_Wild`, `_isHirable=1`.
- 소유 등록 = "고용". 정상 경로는 두 메시지뿐:
  - `TrocTrHireMercenaryFromInventoryReq` 2454 — 인벤 아이템(부적 6종만).
  - `TrocTrHireMercenaryToTargetReq` 2338 — **월드의 대상 액터**. 야생
    포획의 실제 등록 경로로 추정(길들이기 액션차트가 보낼 것).
- 치트 3종(`RequestHireMercenaryForCheatReq` 등)은 스텁.

### 1.2 HireMercenaryToTargetReq 역직렬화 (RVA 0x2960CA0, 정적)

```
헤더 5바이트 검사
읽기 4 -> 대상 핸들 u32      (rbp+0x7f)
읽기 1 -> 플래그 u8          (rbp-0x15)
[세션+0x96] != 0 게이트
0x2ADE280(세션, &결과, &핸들, 플래그)   ; 핸들 -> 액터 (전역 0x6C29C70+0x48 표)
   실패 -> 오류코드 [0x6BB7AE8] "대상 없음"
   액터+0x68 -> +0xB0 -> byte +0x3B0 을 읽어 분기   ; 대상 성질 플래그(미해독)
상태표 조회 0x1398000/0x1398540 ×2  ; FromInventory 와 같은 표. ==2 거부, ==1 필요
세션 vtable+0xE8 -> 컨텍스트
0x26AFF70(rcx=[0x6C29C50], r9=0x11, r8=&태그구조체)   ; 공용 라우터
```

본문 **5바이트**. 소켓·부적 구동과 같은 꼴이라 `resolve_socket_deser` +
게임 스레드 실행 지점 + SEH 가드로 그대로 태울 수 있다. **미확인**:
(a) 상태표의 키가 무엇인지(대상 캐릭터 키? 액터?), (b) `+0x3B0` 플래그가
"길들여짐" 상태인지 캐릭터 고유 성질인지. 둘 다 서버가 길들이기 없이
등록을 거부할 수 있는 지점이다. → §4 Phase 1 캡처로 푼다.

### 1.3 소환 쪽 후보와 판정

| 경로 | 실체 | 판정 |
|---|---|---|
| `SpawnCharacterCheatReq` | 처리기가 아이템 라우터. NPC 증발·무한로딩 냈음 | **금지** (기존) |
| 저수준 `SummonCharacterData` 조립(45필드, 이름 해시 등) | 소환 실행 함수 미특정 | 대공사·고위험 (기존) |
| `TrocTrCatchBySummonReq` 2386 (`u32 A, u32 B`) | 두 값 모두 0x29B3E10 로 **기존 액터 핸들 조회**(핸들 네임스페이스 0x2xxxxxxx 검사). 새 개체를 만들지 않는다 | 소환 아님. "붙잡기" 상태 제어에만 유용 |
| 아이템 사용 `ItemUseData_SummonCharacterWithCatch` (물고기 34종 방생) | 게임이 아이템 사용으로 캐릭터를 소환해 손에 쥐게 하는 **정상 경로**. 객체 0x40바이트에 캐릭터 키가 직접 없고, 소환 태그 해시(+0x30 = 0x5B86A7C3, 두 물고기 동일)로 `SummonItemData` 를 거치는 듯 | **유망하나 RE 필요**: 아이템→캐릭터 대응이 어디 있는지 |
| `SelectMercenarySpawnReq` 2894 (`u64 + float3`) | 이미 가진 동반자의 스폰 위치 선택 | 획득 뒤 "부르기"에 유용 |
| `Skill_CallVehicle/CallDragon` (수집 아이템 2종이 부여) | 가진 탈것 호출 | 획득 뒤 편의 |

### 1.4 액터 핸들을 얻는 방법 (지금 있는 인프라)

- grant.cpp 의 `det_entity_lookup`(엔티티 조회 훅)·`det_actor_getter` 가
  세션→액터를 잡고 있고, 핸들 조회 함수(0x29B3E10 계열)의 표는 전역
  `0x6C29C70+0x48`. 이 표를 걸으면 **월드의 살아 있는 액터 전부**와 그
  캐릭터 키를 얻는다(→ 캐릭터 표로 이름·용병 타입 해석).
- 카메라 초점 좌표·프리캠·`hold`(좌표 눌러쓰기) 인프라가 있어 "가장
  가까운 야생 개체" 계산과 이동이 된다.

## 2. 기능 구성 — "동반자" 패널

```
[동반자]  탭: 목록 | 근처 | 소유
────────────────────────────────────────────────
목록   타입▾ (말/특수/짐승/펫/가축/물고기)   종 검색 [    ]   ☑ 야생만
       키    내부 이름                 타입          [근처에서 찾기] [찾아가기]
       31378 Animal_Black_Horse_Wild   Vehicle_Horse  …
근처   월드 액터 중 동반자 타입인 것 (거리순)
       거리  이름                      상태         [획득] [붙잡기 해제]
소유   ClientMercenaryActorComponent 걷기 (D안)
       이름  타입  [부르기(SelectMercenarySpawn)] 
```

- **목록**: 캐릭터 표 × 용병 표 교차(이미 파이썬으로 한 것을 모드의
  roster 리더에 옮긴다). 한글 이름은 캐릭터 현지화 키 미해결이라 내부
  이름 우선, 도감 보고서 아이템 이름으로 보조.
- **근처**: 액터 표를 주기(1초)로 걸어 동반자 타입만 남기고 플레이어
  거리순 정렬. 여기서 **핸들**이 나온다 — 획득의 입력.
- **찾아가기**: 소환의 대체. 선택한 종의 살아 있는 개체가 근처에
  없으면 "이 종은 현재 로드된 지역에 없음"을 표시한다(스폰 지역 표는
  별도 조사).
- **획득**: 근처 표의 핸들로 2338 구동. 결과는 소유 탭 갱신으로 확인.
- **소환(진짜)**: 마지막 단계. §1.3 의 물고기 방생 경로를 리버스해
  "임의 캐릭터를 손에 쥔 채 소환"이 되면 붙인다.

## 3. 위험과 게이트

- **세이브 오염**: 획득은 실제 게임플레이 거래. 버릴 세이브 필수, SEH,
  1회 1건.
- **거부 경로는 안전**: 역직렬화가 자체 게이트(대상 없음·상태표)에서
  오류코드만 세우고 빠진다. 크래시보다 "아무 일 없음"이 기본 실패 모드다.
- **이중 등록**: 상태표 `==2` 거부가 게임 쪽에 있다.
- **소환 치트 재사용 금지**: 캐릭터 키를 아이템 라우터에 넣는 옛 경로는
  코드에서 제거된 상태를 유지한다.

## 4. 단계 계획 (각 단계에 실측 게이트)

**Phase 0 — 목록·소유 탭 (읽기 전용, 0 위험)**
roster 리더에 용병 타입 매핑(`_mercenaryInfo` 행 번호) 추가, 캐릭터 표에
`_isHirable`·`_Wild` 필터. 소유 탭은 `ClientMercenaryActorComponent`
레코드 구조 실측(probe 로 8개 인스턴스 덤프 → 캐릭터 키 자리 찾기).
게이트: 화면에 목록/소유가 맞게 나온다(스크린샷 대조).

**Phase 1 — 2338 캡처 (플레이 1회)**
`hook_one_socket("TrocTrHireMercenaryToTargetReq")` 를 학습원에 추가하고
야생마(또는 늑대) 길들이기를 **정상 플레이로 1회** 한다. 로그에 5바이트
본문·핸들이 찍히면 페이로드 확정. 같은 세션에서 대상 액터의
`+0x68→+0xB0→+0x3B0` 값을 길들이기 전/후로 읽어 게이트 의미를 푼다.
게이트: 핸들 형식·플래그 값·상태표 키가 문서화된다.

**Phase 2 — 근처 탭 + 획득 구동 (버릴 세이브)**
액터 표 걷기 → 근처 표. 길들이기 **안 한** 야생 개체 핸들로 2338 을
구동해 등록되는지 본다. 되면 로데오 생략 확정. 안 되면 오류코드로
어느 게이트인지 알 수 있다(대상 없음 0x6BB7AE8 / 상태표).
게이트: 소유 탭에 새 개체가 뜨고 게임이 정상.

**Phase 3 — 찾아가기**
근처에 없는 종은 스폰 지역이 필요하다. `SummonCharacterData_TerrainRegionAutoSpawn`
·`_terrainRegionSpawnPerCount`(CharacterInfo +0x2EC) 로 지역을 역추적할
수 있는지 조사. 단순 버전은 "현재 지역의 근처 표"만으로 시작.

**Phase 4 — 진짜 소환 (조사 후 결정)**
물고기 방생의 `SummonItemData`(13필드, `_summonTagNameHash` 등)에서
아이템→캐릭터 대응을 찾고, 임의 캐릭터로 바꿔 소환이 되는지 본다.
되면 "소환 → 근처 → 획득"이 한 흐름이 된다. 안 되면 Phase 3 으로 마감.

## 5. 재사용 코드 지도

| 필요 | 있는 것 |
|---|---|
| 메시지 역직렬화 구동 | `resolve_socket_deser`, 소켓 Phase 2 구동부, 게임 스레드 실행 지점 |
| 메시지 캡처 | `hook_one_socket` / `CDTB_SOCK_DETOUR` |
| 캐릭터·용병 표 | roster.cpp (`+0x30 개수`, `+0x58 레코드`, `+0x08 문자열`) — 용병 매니저 탐지 버그 수정 필요 |
| 액터 열거 | grant.cpp 엔티티 조회 훅, 핸들 표 `0x6C29C70+0x48` (걷기 코드는 새로) |
| 좌표·이동 | camera/freecam, probe `hold`/`findvec3` |
| 소유 읽기 | 인벤토리 컴포넌트 리더 패턴 |

## 6. 하지 않기로 한 것

- 캐릭터 키만으로 "즉시 소유 추가"(월드 개체 없이). 메시지가 없다.
- 부적 스킬(`Active_Hire_*`)을 다른 캐릭터로 돌리기. 스킬 데이터가
  캐릭터를 고정하고, 스킬 표 조작은 상태표와 어긋날 위험이 크다.
- 드래곤·ATAG·배·마차: 야생 개체가 없어 이 기능의 범위 밖.


## 7. Phase 0 진행 (2026-09-05)

구현·빌드·297 테스트 통과. 배포는 게임 실행 중이라 거부됨 → 종료 후
`scripts/deploy.ps1`.

- `game/roster`: 캐릭터 표에서 `_mercenaryInfo`(+0xBE 행 번호)·
  `_isHirable`(+0x156)·`_isCatchable`(+0x148)·`_isUnique`(+0x14B) 를 읽는다.
  용병 표는 `+0x00` 이 포인터라 엄격 검증이 실패하던 버그를 느슨한
  판정(`looks_like_mercenary_manager`)으로 고쳤고, 행 번호를 키로 삼아
  21타입(`_mercenaryType` +0x20)을 읽는다. 이름 규칙 도우미
  `roster_is_wild`/`roster_species`. 단위테스트 `tests/roster_tests.cpp`.
- 패널 "탈것 · 용병 · 캐릭터"에 **동반자 탭** 추가: 타입 콤보(말·특수
  탑승물·탑승물·마차·반려동물·가축·물고기·곤충), "야생만"(기본 켬),
  "고용 가능만", 검색. 줄을 누르면 키가 클립보드로. 기존 "용병" 탭은
  "용병 타입"으로 바뀌어 21행을 보여준다.
- **소유 탭은 미구현.** `ClientMercenaryClanActorComponent`(1개)·
  `ClientMercenaryActorComponent`(10개)를 등록된 6종의 캐릭터 키로 걸어
  봤으나 소유 목록 배열을 특정하지 못했다(u16 키 스캔은 잡음이 많다).
  다음 수는 서버가 로드 시 보내는 `TrocTrMercenaryDataListAck` 역직렬화를
  캡처해 형식을 얻는 것 — 그런데 소켓 캡처 훅 인프라(`hook_one_socket`)가
  e2948b5 에서 제거돼 다시 들여와야 한다.

### 소켓의 "완전자동 불가" 결론이 여기에도 적용되는가

소켓이 막힌 이유는 **컨테이너 핸들(0xB010 네임스페이스)** 이 핸들 월드
객체에만 있고 모드가 읽는 서버 인벤토리와 링크가 없어서였다. 획득
메시지 2338 이 요구하는 것은 **액터 핸들(0x2xxxxxxx 네임스페이스,
0x29B3E10 이 전역 표 `0x6C29C70+0x48` 로 조회)** 이고, 이 핸들은 모드가
이미 잡고 있는 엔티티 조회 훅(`det_entity_lookup(mgr, out, id)`)의
`id` 로 지나간다. 즉 **핸들을 얻는 길이 소켓과 다르다** — 훅 로그로
"근처 액터 핸들 → 액터 → 캐릭터 키"를 세션 안에서 학습할 수 있다.
따라서 Phase 1(캡처)은 소켓 훅 복원 없이도 엔티티 조회 훅 확장으로
시작할 수 있다. 서버 게이트(+0x3B0 플래그·상태표)가 길들이기 상태를
요구하는지는 여전히 미확인이다.

### 게임 탭에 들어가면 소환은 따라온다 (확인)

부적으로 등록한 6종이 게임의 특수 탑승물/반려동물 탭에 나타났고, 그
탭의 부르기(`Skill_CallVehicle`/`ClientFrameEventCallMercenary`/
`TrocTrCallSpecialVehicleByQuickSlotReq`)는 **등록 목록**을 대상으로
한다. 획득이 같은 목록(서버 `MercenaryDataList`)에 들어가는 한, 소환은
게임 UI 가 해 준다. 그래서 이 기능은 "소환"을 직접 만들 필요가 없고
**획득만 성공시키면 된다** — Phase 4(진짜 소환)의 우선순위를 더 낮춘다.


## 8. Phase 1 진행 (2026-09-05) — 캡처 훅 배포됨

`game/companion` 모듈(새 파일). 다른 세션이 제거한 소켓 캡처 훅과 같은
방식(서술자 vtable[2] 역직렬화 후킹, 패킷 +0x10 길이·+0x18 페이로드)을
독립 모듈로 다시 만들었다. 9경로를 건다:

| 태그 | 메시지 | 기대 |
|---|---|---|
| 획득/대상 | TrocTrHireMercenaryToTargetReq 2338 | 길들이기 성공 시. 본문 `u32 핸들 + u8` 을 풀어 `획득 대상: 액터 핸들 …` 로 로그 |
| 획득/아이템 | TrocTrHireMercenaryFromInventoryReq 2454 | 부적 사용 시 |
| 붙잡기 | TrocTrCatchBySummonReq 2386 | 포획 시작/유지 |
| 등록이벤트 | TrocTrFrameEventRegistMercenaryReq | 액션차트가 보내는 등록 |
| 스폰선택 | TrocTrSelectMercenarySpawnReq 2894 | 부르기 위치 |
| 부르기 | TrocTrCallSpecialVehicleByQuickSlotReq | 게임 탭에서 부르기 |
| 소유목록 | TrocTrMercenaryDataListAck | **로드 시 서버가 보내는 소유 목록 — 소유 탭의 와이어 형식** |
| 등록후소환 | TrocTrSummonMercenaryAfterRegistAck | 등록 직후 소환 |
| 획득응답 | TrocTrResponseHiredMercenaryToTargetAck | 2338 의 응답 |

로그는 `동반자 캡처 [태그] ID n 본문 n바이트 (전체 n): hex` (최대 80건,
768바이트까지). 순수 디코더(`decode_message_header`,
`decode_hire_to_target`, `hex_bytes`)는 단위테스트 4개. 빌드·301 테스트
통과, **17:42 배포**(게임이 꺼져 있었다). `camera.cpp` 의 분석 루프에서
`companion_capture_install` 을 부른다.

**남은 실측(플레이):** 게임을 켜면 로드 시 `소유목록` 이 먼저 찍혀야
한다(안 찍히면 Ack 는 다른 경로로 역직렬화되는 것). 그 다음 야생마나
늑대를 한 마리 길들이면 `획득/대상` 이 찍히고, 그 5바이트로 §1.2 의
페이로드 가정이 확정된다.

### 외부 참조: Nexus "Crimson Desert Enhanced Ultimate" CT v4.0 / ASI v5.0 (2026-09-04, 게임 2.0.0.2 빌드 2692)

사용자가 받아 둔 두 파일(`E:\CDToybox\*.zip`, 스크래치 `nexus_ct`,
`nexus_asi` 에 풀어 둠)에서 이 기능과 맞닿는 것만 옮긴다. 남의 코드라
**시그니처·오프셋은 참고값**이고 우리 exe 에서 재검증해야 한다.

1. **즉시 포획 (Instant Horse Capture).** 길들이기 미니게임의 게이지
   저장 지점을 패치한다. AOB `C5 FA 11 13 0F 9A D0 3C 01`
   (`vmovss [rbx],xmm2 · setp al · cmp al,1 · jz · vminss xmm1,xmm2,[rbx+1C]`).
   `[rbx+0x1C]` 가 완료 임계, `[rbx]` 가 진행도라 임계값을 진행도에 써서
   즉시 완료시킨다. 부작용: 켜 둔 동안 **근처의 차오르는 게이지 전부**가
   완료된다(설명에 명시). → **획득의 대안 경로**: 메시지를 조립하지 않고
   게임 자신의 길들이기를 즉시 끝내면 게임이 스스로 2338 을 보낸다.
   플레이어가 야생 개체에게 포획을 시작해야 하는 조건은 그대로다.
2. **최대 신뢰도 (Max Trust, 사람·펫).** 구조 복사 지점
   (`vmovups [rcx+20],ymm1`, AOB `4E ?? … 20 C5 FC 11 49 20`) 뒤에서
   레코드 **+0x28 QWORD 신뢰도**를 게임이 쓴 값의 10배(상한 100)로
   덮는다. 옛 버전은 상수 100 을 직접 써서 크래시·2001개 무관 값 오염을
   냈고, "게임이 만든 값만·위로만" 규칙으로 고쳤다. 펫 획득(친밀도)의
   가속 경로. 말 신뢰도(상한 350)는 훅 자리 한가운데로 점프가 들어와
   **패치 불가**로 거부돼 있다(site+4·site+7 이 분기 대상).
3. **살아 있는 캐릭터 목록 걷기** (`CD_CharList`) — "근처" 탭의 바탕.
   캐릭터 매니저 전역은 5개 앵커 AOB 의 투표로 찾는다:
   `48 8B 05 ?? ?? ?? ?? 48 8B 08 4D 8B 00 49 C1 E8 20` 등(`mov rax,[rip+g]`
   의 rel32 를 풀어 g). `g → [g] → [[g]]` 가 매니저, 매니저 **+0xB8 포인터
   배열, +0xC0 u32 개수**(상한 8192). 원소 = 살아 있는 캐릭터.
   캐릭터 `+0x68` 액터 → `+0x20` → `+0x18` → `+0x58` 스탯 게이지 배열
   (0x90 간격, +0x00 타입 u32, +0x08 현재, +0x18 기본, +0x30 상한;
   entry[0] 이 타입 0 체력이어야 진짜). 게이지 타입 19 = 탈것 질주,
   48 = 탈것 능력(화염), 17·18 은 열/연소라 절대 고정 금지.
   우리 쪽은 RTTI 로 매니저를 찾는 편이 낫다(앵커 AOB 는 빌드마다 밀림).
   캐릭터 객체 안의 **CharacterInfo 키/포인터 위치와 액터 핸들 위치**는
   이 표에도 없다 — 우리가 실측해야 한다.
4. 그 밖: 지급은 "게임의 create 경로"로 다시 만들어 저장되는 진짜
   아이템이 됐다고 함(ASI v5.0). 가방 확장은 732 초과 시 게임이 깨진다
   (다른 세션이 오늘 되돌린 가방 확장과 같은 현상).

### 판정 갱신

- **Phase 2 의 획득 경로가 둘이 됐다.** (a) 2338 역직렬화 구동(핸들 필요,
  서버 게이트 미확인), (b) 즉시 포획 패치(게임 흐름 그대로, 근처 게이지
  전부 완료 부작용). (b) 가 더 낮은 위험으로 보이며, 켜고 끄는 토글로
  구현하면 부작용은 사용자가 통제한다. 캡처 결과를 본 뒤 고른다.
- 펫은 (b) 의 짝인 신뢰도 가속으로 같은 틀에 들어간다.


## 9. 살아 있는 액터 → 캐릭터 신원 (2026-09-05, 실측·프로브 검증) — "근처" 탭 구현

사용자 조작 없이 진행한 부분. 블라인드 포인터 스캔(캐릭터 키·레코드
포인터·이름 문자열·매니저 포인터, 깊이 3)은 전부 실패했고, **게임의
메타 시스템에 등록된 접근 함수**를 따라가 풀었다.

### 도출 경로

1. RTTI 클래스 `GameMeta_ClassFunctionTemplate<const StaticInfoWrapper<CharacterKey>&, CommonActor>`
   의 인스턴스(0x453B674BFD0) +0x20 에 멤버 함수 포인터 → **RVA 0x17544D0**:
   `mov rax,[rcx+0x68]; mov rax,[rax+0x20]; add rax,0x30; ret`.
2. 호출부(0x29B7707) 바로 뒤의 **RVA 0x383200** 이 래퍼를 푼다:
   `movzx edi, word [rcx]` → `[전역 0x6C29FF8]` 의 `+0x08 개수`·`+0x58 레코드
   배열` 로 `records[edi]`. 즉 래퍼 첫 u16 은 **키가 아니라 레코드 배열
   행 번호**이고, 전역 **0x6C29FF8 = CharacterInfoManager 포인터**다
   (RTTI 로 찾은 인스턴스와 일치 확인).
3. 따라서 액터 `a` 의 캐릭터 = `character_rows[ *(u16*)([[a+0x68]+0x20]+0x30) ]`.
   클라이언트 액터 157/157, 서버 액터도 같은 사슬로 풀린다.

### ClientActorManager (RTTI 인스턴스 1개)

`+0x128` 부터 `0x10` 간격으로 `{포인터 배열 u64, u32 A, u32 용량}` 버킷
(용량 4000/1000/2000/3000/16/100/10/500/50/30/20 …, 액터 종류별). **u32 A
는 살아 있는 수가 아니다** — 같은 세션에서 130 → 0 으로 바뀌는 동안 배열은
212개 그대로였다. 배열은 빽빽한 포인터 목록이고 끝은 0 또는 쓰레기라,
용량까지 힙 포인터로 보이는 동안 걷는다.

### 구현

- `game/actors.{h,cpp}`: `find_actor_manager`(RTTI+검증), `walk_actor_pointers`,
  `actor_character_row`, `snapshot_live_actors`, 모드 캐시
  (`discover_actor_manager` 는 분석 루프, `refresh_live_actors` 는 그리는
  스레드가 2초/버튼). 단위테스트 4개.
- `game/roster`: `RosterEntry.row` + `character_by_row()` (널 슬롯 때문에
  항목 순번 ≠ 행 번호).
- 패널 "탈것 · 용병 · 캐릭터" 에 **근처 탭**: 액터 주소·내부 이름·타입·
  야생·고용가능, "동반자만" 필터, 줄 클릭으로 액터 주소 복사.
- `probe nearby [all]` — 모드와 같은 코드로 라이브 검증:

```
액터 249개, 이름 풀림 131, 동반자 타입 27
  0x45488FD6400  행 4074  키 31378  타입행 1  Animal_Black_Horse_Wild_31378  [야생]  [고용가능]
  0x45488FD85C0  행    3  키     4  타입행 0  Damian
  0x4538339CA80  행 6799  키 20878  타입행 5  Riding_Wolf_1001
  … 부엉이·까마귀·비둘기·나방·NPC(Luke/Ronald/Marius/Carl/Russo) …
```

플레이어 근처에 **전설마 흑마(31378)** 가 실제로 있다 — Phase 2 획득
실험의 첫 대상 후보. 이름이 안 풀리는 118개는 기믹(`Object_Gimmick_*`)
등 캐릭터 홀더가 없는 액터다.

### 캡처 훅 동작 확인

로그 18:40: `동반자 캡처 [스폰선택] ID 2894 본문 20바이트:
4E 0B 00 14 00 | E1 43 0F 00 00 00 00 00 | 5B 01 24 C6 63 BA 18 44 73 79 88 C5`
(부르기/스폰 선택 시). 본문 = **u64 MercenaryNo(0xF43E1, 0xF43E2 두 마리) +
float3 위치**(-10496.3, 610.9, -4367.2). 요청(Req) 메시지는 싱글플레이에서도
와이어 직렬화를 타므로 **2338(획득/대상)도 길들이기 때 잡힌다.** 응답(Ack)은
로드 때 안 잡혔다 — 로컬 서버가 객체로 넘기는 듯. 소유 목록은 Ack 캡처
대신 다른 길(MercenaryNo ↔ 캐릭터 대응은 부르기 캡처로 조금씩 학습 가능).

### Nexus 참조 AOB 는 우리 exe 에 없다

CT v4.0 의 즉시 포획·신뢰도·캐릭터 매니저 앵커 AOB 전부 0건(그쪽 빌드
2692용). 참고값으로만 남긴다.

### 남은 것

- 액터 **핸들**(2338 본문의 u32) ↔ 액터 포인터 대응. 후보: 핸들 조회 함수
  0x29B39D0(`[0x6C29C70]+0x48` 표)의 역, 또는 엔티티 조회 훅에서 (id, 결과)
  쌍 학습. 길들이기 1회 캡처가 오면 그 핸들로 표를 역추적한다.
- 액터 월드 좌표(거리 정렬). 상태 컴포넌트 근처.
- 배포: 게임 실행 중이라 대기.


## 10. 메시지 구동 인프라 복원 + 2976 실측 (2026-09-06)

"길이 없으면 만든다" — 제거됐던 역직렬화 구동을 되살려 **범용 메시지 구동**으로
만들고, 아이템 사용 메시지를 실제로 구동했다. 게임은 흔들리지 않았고(3회 구동,
크래시·교착 없음) 새 도구가 셋 남았다.

### 10.1 범용 메시지 구동 (grant)

- `resolve_message(rtti, reader, "TrocTr...Req", &MessageDesc)` — RTTI → vtable →
  정적 초기화(`lea rax,[rip+vtable]` + `mov [rip+desc],rax`)로 서술자를 찾고
  `vtable[2]` 를 역직렬화로 잡는다. 소켓 Phase 2 의 `resolve_socket_message` 를
  일반화한 것.
- `request_message(session, msg, wire, len)` — 지급과 **같은 대기열**(게임 스레드
  실행 지점·SEH·2초 쿨다운)에 걸어 `run_message` 가 구동한다. 패킷은
  `[+0x00] 세션 · [+0x10] u16 길이 · [+0x18] 페이로드 · [+0x38] 0`.
- 와이어 머리 5바이트 = `[ID u16][00][본문길이 u16]`.

### 10.2 명령 파일 (오버레이 클릭 없이 실험)

DLL 옆 `cdtoybox_cmd.txt` 에 한 줄 쓰면 모드가 읽고 지우고 실행한다.

```
give 1003843 1            # 지급 (검증됨: 부적이 인벤에 들어옴)
useitem <A> <B> <C> <D>   # 2976 구동
```

세션은 그란트 패널과 같은 규칙(서버 세션 중 가장 유력한 것)으로 고른다.
이것으로 **분석 도구가 게임에 직접 실험을 걸 수 있다** — 지금까지 사람이
오버레이를 눌러야 했던 자리를 대체한다.

### 10.3 `TrocTrUseItemByItemInfoReq` (2976) 본문 해독

역직렬화 RVA 0x29373D0 이 읽는 13바이트: `u32 A, u32 B, u8 C, u32 D`.

| 자리 | 검사 | 뜻 |
|---|---|---|
| A | `test ebx,ebx; je 실패` 뒤 **ClientActorManager vtable+0x28(=0x836C20) 으로 핸들 조회** | **액터 핸들** |
| B | `test edx,edx; je 실패` | 0 이면 안 되는 u32 (개수/플래그) |
| C | `cmp C,0x0D; je → 오류코드` | **0x0D 는 오류 경로**. 다른 값이어야 한다 |
| D | `0x384730` 이 제자리에서 변환, 하위 u16 == 0xFFFF 면 오류 | **아이템 키**(→ 짧은 식별자. 부적 1003843 → 순번 2700) |

성공 경로는 `0x2A711D0(세션액터, &D, 핸들액터, D_u16, 0xFFFF, &C, &D복사)` 를
부르고, 그 함수가 D 자리에 써 놓은 값이 0 이면 성공이다. **오류는 반환값이 아니라
0x3f5 태그 + 공용 라우터(0x26AFF70)로 UI 메시지를 보내는 방식**이라, 우리가 찍는
`결과 0` 은 성공 신호가 아니다(머리 검증 실패에만 쓰인다).

### 10.4 핸들 월드를 열었다 (소켓을 막았던 그 문제)

`ClientActorManager` = `[0x6C29AA8] + 0x10`, 컨테이너 = 매니저 `+0x08`
(`ClientActorContainer`).

```
컨테이너 +0x88 버킷 수 · +0x8C 항목 수 · +0x98 버킷 배열 · +0xA0 노드 포인터 배열
버킷 = 0x100 바이트 = { u32 개수, ..., +0x08 부터 {u32 키, u32 색인} 쌍 }
노드 = { u32 ?, +0x04 핸들 키, +0x08 액터 포인터 }
조회 함수 0x3F4320: idx = 키 % 버킷수, 버킷 안 선형 탐색, 노드+4 == 키 확인
```

실측(2026-09-06): 핸들 441개, 이름 해석 440개. **플레이어 = `ClientUserActor`,
핸들 `0x90100000`**(사용자 액터는 `0x9010` 네임스페이스, 일반 액터는 `0xB010`).
소켓 문서가 "핸들이 객체에 저장돼 있지 않아 역조회 불가"라고 결론지었던 그
핸들 월드를 이제 **걸어서 액터 ↔ 핸들 양방향으로 풀 수 있다**.

### 10.5 구동 실측 — 아직 먹지 않는다

3회 구동(모두 무사, 게임 정상):

| A | B | C | D | 결과 |
|---|---|---|---|---|
| 1003843 | 0 | 0x0D | 0 | B==0 → 실패 경로(정적 확인) |
| 0xB0100A8E | 1 | 0 | 1003843 | 부적 소모 없음 |
| 0x90100000 (플레이어) | 1 | 0 | 1003843 | 부적 소모 없음 (인벤 1개 → 1개) |

막힌 지점 추정: (a) C 의 올바른 값을 모른다(0 이 유효한지 불명), (b) 역직렬화가
TLS 바이트(`[TLS+0x1EC]`)로 **클라이언트/서버 매니저를 갈라 잡는데**, 게임 스레드에서
서버 쪽이 잡히면 클라이언트 핸들은 조회에 실패한다. 서버 컨테이너
(`[0x6C29AB0]+0x10 → +8`, `CommonActorContainer`)는 배치가 클라이언트와 달라
아직 못 걸었다(버킷 후보 스캔 실패).

### 10.6 남은 한 걸음은 실측 1회

성공 신호가 없는 4필드를 눈감고 훑는 것은 실제 게임플레이 거래를 반복하는 것이라
하지 않는다. **게임에서 부적을 한 번 쓰면** 훅이 그 자리에서:

- `아이템사용` 또는 `아이템사용/정보` — A·B·C·D 의 정답 값
- `등록이벤트`(FrameEventRegistMercenary) — **부적이 어느 캐릭터를 등록하는지**
  (§8 에서 정적 표에 없다고 확정한 그 답)

를 찍는다. 그 한 번이면 가상 부적의 성립 여부가 결론난다.


## 11. 획득 성공 — 부적 없이 동반자를 등록했다 (2026-09-06, 실측)

`TrocTrHireMercenaryToTargetReq`(2338) 구동으로 **부적이 없는 종 4마리를
실제로 등록**했다. 서버가 획득 응답과 새 용병 번호를 돌려줬다.

### 11.1 경로 (전부 실측 확인)

```
명령 파일 `msg 22 09 00 05 00 <핸들 u32 LE> <플래그 u8>`
 └ 범용 구동 → 2338 역직렬화 0x2960CA0 (게임 스레드, SEH)
    ├ 게이트: [세션+0x96] != 0                      (우리 세션 = 1, 통과)
    ├ 컴포넌트: [[세션+0x68]+0x110]                  (ServerMercenaryClanActorComponent)
    ├ 핸들 == 0 이면 작업 호출 없이 종료             (실측: 로그 없음)
    └ **고용 작업 RVA 0x2ADE280**(컴포넌트, &결과, &핸들, 0, 플래그)
         결과 0 → 성공, 서버가 `TrocTrResponseHiredMercenaryToTargetAck`(2107) 발신
         결과 != 0 → 그 코드를 0x3f5 태그로 클라이언트에 오류 알림
```

획득 응답 2107 본문 20바이트: `{u32 사용자핸들, u32 대상핸들, u32 사용자핸들,
u64 MercenaryNo}`. MercenaryNo 는 부르기(2894)가 쓰는 것과 같은 번호 공간이다.

### 11.2 실측 표 (2026-09-06 10:39~10:42)

| 대상 | 용병 타입 | 결과 |
|---|---|---|
| `Animal_Jindodog_Domestic_02` | Pet | **성공** → No 1000972 |
| `Animal_Lumif_Domestic_Saddle_32514` | Vehicle_Horse | **성공** → No 1000973 |
| `Animal_RussianBlue_Cat_Domestic` | Pet | **성공** → No 1000974 |
| `Animal_Sparrow_Wild_30071` | Pet | **성공** → No 1000975 |
| `Animal_Owl_Wild_30060` | Pet | 거부 `0xD65F8D70` |
| `Animal_Black_Horse_Wild_31378` (유니크 전설마) | Vehicle_Horse | 거부 `0x97AE29C9` |
| `Damian` (스토리 동료) | Mercenary_Main | 거부 `0x97AE29C9` |
| 핸들 0 | - | 작업 호출 전 차단 |

거부 코드는 두 종류뿐이었다. 야생/사육 구분이 아니다 — **야생 참새는 성공**했고
사육 개·고양이·말도 성공했다. 거부된 것은 (a) 부엉이(공중/거리 추정),
(b) 유니크 전설마와 스토리 동료(고용 불가 유형 추정). 플래그 0/1 은 결과를
바꾸지 않았다.

### 11.3 사용자 질문에 대한 답

**"부적 없는 종에 부적 같은 아이템을 만들 수 있나"** → **아이템을 만들 필요가
없다.** 부적은 등록의 한 입구일 뿐이고, 등록 자체는 2338 로 직접 열린다.
부적 6종에 묶이지 않고 **월드에 있는 동반자 타입 액터면 대체로 등록된다**
(탈것 타입 말 포함).

부적 자체를 흉내내는 길이 막힌 근거도 함께 확정됐다: 부적 사용(2676)은
**고용 작업 0x2ADE280 을 부르지 않는다**(훅에 아무것도 안 잡힘). 즉 부적의
등록은 다른 경로이고, 그 경로의 아이템↔캐릭터 대응은 정적 표에 없다(§8).

### 11.4 남은 것

- 등록된 4마리가 게임 UI(반려동물·특수 탑승물 탭)에 뜨는지 화면 확인.
- 거부 코드 두 개의 이름. 런타임 초기화 전역(0x6BB7xxx)에 담긴 해시로 보인다.
- 되돌리기: `TrocTrDisbandMercenaryReq`·`TrocTrFireMercenaryReq` 를 같은 방식으로
  구동하면 될 것. 아직 안 해 봤다.
- 부엉이 거부(0xD65F8D70)가 거리 조건이면, 가까이 가서 다시 걸면 성공할 수 있다.
