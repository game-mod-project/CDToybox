# 오버레이 UI/UX 3단계 (보관함 저장 경로 · 큐 · 배치) 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스펙 5절(T3)을 구현한다 — 보관함 변경을 1초 뒤 자동 저장하고, 일괄 지급 큐를 창 밖(`stash_tick`)으로 옮겨 창을 닫아도·세션이 없어도 이어지게 하며, 담기 목적지를 이름으로 잡고, 즐겨찾기/세트 배치와 확인·툴팁·로그를 다른 창과 같은 규격으로 맞춘다.

**Architecture:** ImGui 없는 조각 `render/stash_queue.{h,cpp}`(큐 상태 기계 `stash_queue_step` + 자동 저장 판정 `stash_autosave_due`)와 `game::Stash::find_set`(이름→번호)을 먼저 만들어 시험으로 못 박고, `stash_panel.cpp` 는 그것을 쓰는 얇은 층으로 바꾼다. `overlay::draw_windows()` 가 매 프레임 `stash_tick()` 을 불러 창이 닫혀도 큐·자동 저장이 돈다. 결과 줄은 1단계 `Notice`, 확인은 `confirm_small_button`, 색은 `col::` 만 쓴다.

**Tech Stack:** C++20 / MSVC 19.44, CMake + Ninja (`scripts/build.ps1`), Dear ImGui 1.92.9b, 자체 테스트 하니스(`tests/harness.h`: `TEST`/`CHECK`/`CHECK_EQ`).

**Spec:** `docs/superpowers/specs/2026-09-10-overlay-uiux-design.md` (5절 전체, 6절 검증의 "3단계" 항목, 0절의 T3 6건)

## Global Constraints

- 작업 트리 **`E:/CDToybox-sdd`**(브랜치 `feat/uiux-p3`, `develop@a560e4b` 에서 분기). 모든 git·파일 명령에 절대경로 또는 `-C E:/CDToybox-sdd`. `E:/CDToybox`·`E:/CDToybox-input` 은 건드리지 않는다. `cd` 뒤 상대경로 명령을 이어 내지 말 것(호출 사이 cwd 가 리셋된다).
- 빌드 `pwsh -NoProfile -File E:/CDToybox-sdd/scripts/build.ps1`, 테스트 `E:/CDToybox-sdd/build/cdtb_tests.exe`. 기준선 **438 tests, 0 failures**. 새 `.cpp` 는 `CMakeLists.txt` 의 해당 목록에 직접 넣는다(GLOB 없음). 헤더만 있는 조각은 목록에 안 넣는다.
- 화면 문구 "-습니다" 체. 상태 색은 `col::kOk/kWarn/kBad/kBusy` 만(등급 색 `grade_color`, 비활성 회색 `TextDisabled` 예외). 실패 `Bad`, 밀림·한쪽만 `Warn`, 성공 `Ok`, 진행 중 `Info`/`kBusy`.
- 게임 메모리 쓰기 경로(`request_give` 의 호출 조건·인자 조립 — 담금질·연마·내구도·소켓 상한 자르기)는 **그대로 옮긴다**. 새 기능 없음 — 스펙에 없는 코드를 추측 구현하지 말 것; 명세가 부족하면 `NEEDS_CONTEXT`.
- 시각은 `ImGui::GetTime()`(초, double). 자동 저장 지연 1.0초, 큐 간격 2.0초(스펙).
- ImGui 위젯 ID: 세트 줄은 `PushID(1000 + i)`·항목 `PushID(j)` 규칙을 유지한다(정렬 없음).
- 커밋 메시지 한국어, 끝에 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. 태스크당 한 커밋.
- Edit 도구가 훅에 막히면 python(`C:/Users/deepe/AppData/Local/Python/pythoncore-3.14-64/python.exe`) 정확 문자열 치환(발생 1회 단언). bash 히어독으로 C++ 를 쓰지 않는다 - Write 도구로 파일을 쓴다.
- 부르는 쪽이 `ImGui::End()` 하는 규약(`begin_window` · `loading_gate`)은 그대로.

---

## 파일 구조

| 파일 | 역할 | 컴파일 |
|---|---|---|
| `src/render/stash_queue.h` / `.cpp` (신규) | `StashQueue`, `QueueStep`, `stash_queue_step/sent/clear/remaining`, `stash_autosave_due` — ImGui 없음 | DLL + tests |
| `tests/stash_queue_tests.cpp` (신규) | 위 조각의 시험 7개 | tests |
| `src/game/stash.{h,cpp}` | `Stash::find_set(name)` | core(+tests) |
| `tests/stash_tests.cpp` | `find_set` 시험 3개 | tests |
| `src/render/stash_panel.{h,cpp}` | 자동 저장 · `stash_tick` · 큐를 `StashQueue` 로 · 이름 목적지 · Notice · 배치 · 로그 · `stash_queue_progress/cancel` | DLL |
| `src/render/overlay.cpp` | `draw_windows()` 첫 줄 `stash_tick()`, 본창 진행 줄 | DLL |
| `docs/STATUS.md` | 보관함 설명·시험 수 | 문서 |

스펙 서명에서 한 가지를 바꾼다: `stash_queue_step(q, have_session, now, interval)` 의 `interval` 은 step 안에서 쓸 곳이 없다(다음 시각은 보낸 직후에 잡는다). 그래서 `stash_queue_sent(q, now, interval = 2.0)` 로 옮긴다. 판정: 컨트롤러(플랜 작성 시).

---

### Task 1: 큐 상태 기계와 자동 저장 판정 (`stash_queue.{h,cpp}`)

**Files:**
- Create: `src/render/stash_queue.h`, `src/render/stash_queue.cpp`, `tests/stash_queue_tests.cpp`
- Modify: `CMakeLists.txt`(`cdtoybox` 목록 `src/render/stash_panel.cpp` 다음 줄, `cdtb_tests` 목록 `tests/stash_tests.cpp` 다음 줄과 `src/render/notice_state.cpp` 다음 줄)

**Interfaces:**
- Consumes: `game::StashEntry`(`game/stash.h`).
- Produces (Task 3 이 쓴다):
  - `struct StashQueue { std::vector<game::StashEntry> items; std::size_t at; double next_at; bool no_session_noted; }`
  - `enum class QueueStep { Idle, Wait, Send, Done, NoSession }`
  - `QueueStep stash_queue_step(StashQueue*, bool have_session, double now)`
  - `void stash_queue_sent(StashQueue*, double now, double interval = 2.0)`
  - `void stash_queue_clear(StashQueue*)`, `std::size_t stash_queue_remaining(const StashQueue&)`
  - `bool stash_autosave_due(double dirty_at, double now, double delay = 1.0)` (Task 2 가 쓴다)

- [ ] **Step 1: 시험을 먼저 쓴다** — `tests/stash_queue_tests.cpp`

```cpp
#include <cstddef>

#include "render/stash_queue.h"
#include "harness.h"

namespace {

using cdtb::render::QueueStep;
using cdtb::render::StashQueue;
using cdtb::render::stash_autosave_due;
using cdtb::render::stash_queue_clear;
using cdtb::render::stash_queue_remaining;
using cdtb::render::stash_queue_sent;
using cdtb::render::stash_queue_step;

StashQueue two_items() {
    StashQueue q;
    q.items = {cdtb::game::StashEntry{1, 1}, cdtb::game::StashEntry{2, 1}};
    return q;
}

}  // namespace

TEST(stash_queue_is_idle_when_empty) {
    StashQueue q;
    CHECK(stash_queue_step(&q, true, 0.0) == QueueStep::Idle);
    CHECK(stash_queue_step(nullptr, true, 0.0) == QueueStep::Idle);
    CHECK_EQ(stash_queue_remaining(q), 0u);
}

// 보낸 직후에는 간격만큼 기다리고, 간격이 차면 다음 것을 보낸다.
TEST(stash_queue_sends_then_waits_for_the_interval) {
    StashQueue q = two_items();
    CHECK(stash_queue_step(&q, true, 10.0) == QueueStep::Send);
    stash_queue_sent(&q, 10.0, 2.0);
    CHECK_EQ(q.at, 1u);
    CHECK(stash_queue_step(&q, true, 11.9) == QueueStep::Wait);
    CHECK(stash_queue_step(&q, true, 12.0) == QueueStep::Send);
}

// 세션이 없으면 큐를 버리지 않는다 - 세션이 돌아오면 그 자리에서 이어진다.
TEST(stash_queue_keeps_items_without_a_session) {
    StashQueue q = two_items();
    CHECK(stash_queue_step(&q, false, 10.0) == QueueStep::NoSession);
    CHECK_EQ(stash_queue_remaining(q), 2u);
    CHECK(stash_queue_step(&q, true, 10.0) == QueueStep::Send);
}

// 마지막 것까지 보낸 뒤 첫 호출이 Done 이고 그때 큐가 비워진다. 그 다음은 Idle.
TEST(stash_queue_reports_done_once_then_idle) {
    StashQueue q = two_items();
    stash_queue_sent(&q, 0.0);
    stash_queue_sent(&q, 2.0);
    CHECK(stash_queue_step(&q, true, 4.0) == QueueStep::Done);
    CHECK(q.items.empty());
    CHECK(stash_queue_step(&q, true, 4.0) == QueueStep::Idle);
}

TEST(stash_queue_clear_resets_everything) {
    StashQueue q = two_items();
    q.no_session_noted = true;
    stash_queue_sent(&q, 5.0);
    stash_queue_clear(&q);
    CHECK(q.items.empty());
    CHECK_EQ(q.at, 0u);
    CHECK(!q.no_session_noted);
    CHECK_EQ(q.next_at, 0.0);
}

TEST(stash_queue_remaining_counts_the_unsent) {
    StashQueue q = two_items();
    CHECK_EQ(stash_queue_remaining(q), 2u);
    stash_queue_sent(&q, 0.0);
    CHECK_EQ(stash_queue_remaining(q), 1u);
    stash_queue_sent(&q, 2.0);
    CHECK_EQ(stash_queue_remaining(q), 0u);
    stash_queue_sent(&q, 4.0);   // 끝을 넘겨 불러도 at 은 더 안 오른다
    CHECK_EQ(q.at, 2u);
}

TEST(stash_autosave_is_due_one_second_after_the_change) {
    CHECK(!stash_autosave_due(-1.0, 100.0));   // 변경 없음
    CHECK(!stash_autosave_due(10.0, 10.5));
    CHECK(stash_autosave_due(10.0, 11.0));
    CHECK(!stash_autosave_due(10.0, 10.9, 1.0));
    CHECK(stash_autosave_due(10.0, 15.0, 5.0));
}
```

- [ ] **Step 2: CMake 에 넣고 빌드해 실패를 본다**

`CMakeLists.txt` 세 곳:
- `cdtoybox` 목록: `    src/render/stash_panel.cpp` 다음 줄에 `    src/render/stash_queue.cpp`
- `cdtb_tests` 목록: `    tests/stash_tests.cpp` 다음 줄에 `    tests/stash_queue_tests.cpp`
- `cdtb_tests` 목록: `    src/render/notice_state.cpp` 다음 줄에 `    src/render/stash_queue.cpp`

Run: `pwsh -NoProfile -File E:/CDToybox-sdd/scripts/build.ps1`
Expected: FAIL — `render/stash_queue.h` 없음.

- [ ] **Step 3: 구현** — `src/render/stash_queue.h`

```cpp
#pragma once

#include <cstddef>
#include <vector>

#include "game/stash.h"

namespace cdtb::render {

// 보관함 일괄 지급 큐. ImGui 없음 - 창이 닫혀도 overlay 가 매 프레임
// stash_tick() 으로 돌린다. 지급 요청(request_give)은 레인 쿨다운(2초)이 있어
// 한 번에 다 못 보낸다 - 한 개씩, 간격을 두고 흘려보낸다. 보내는 일 자체는
// 이 파일 밖(stash_panel)이 한다 - 여기는 "이번 프레임에 무엇을 할지" 만 정한다.
struct StashQueue {
    std::vector<game::StashEntry> items;
    std::size_t at = 0;              // 다음에 보낼 항목
    double next_at = 0.0;            // 이 시각 전에는 보내지 않는다
    bool no_session_noted = false;   // 세션 없음 경고를 한 번만 내려고
};

enum class QueueStep {
    Idle,        // 큐가 비어 있다
    Wait,        // 간격을 기다린다
    Send,        // items[at] 을 지금 보내라 - 보냈으면 stash_queue_sent()
    Done,        // 마지막까지 보냈다 - 한 번만 나고, 그때 큐가 비워진다
    NoSession,   // 세션이 없다 - 큐는 남긴다
};

QueueStep stash_queue_step(StashQueue* q, bool have_session, double now);
// items[at] 을 보냈다. at 을 올리고 다음 시각을 now + interval 로 잡는다.
void stash_queue_sent(StashQueue* q, double now, double interval = 2.0);
// 큐를 비운다(중단·완료).
void stash_queue_clear(StashQueue* q);
// 아직 보내지 않은 항목 수.
std::size_t stash_queue_remaining(const StashQueue& q);

// 자동 저장 판정. dirty_at 은 마지막 변경 시각(음수면 변경 없음). delay 초가
// 지났으면 true.
bool stash_autosave_due(double dirty_at, double now, double delay = 1.0);

}  // namespace cdtb::render
```

`src/render/stash_queue.cpp`

```cpp
#include "render/stash_queue.h"

namespace cdtb::render {

QueueStep stash_queue_step(StashQueue* q, bool have_session, double now) {
    if (q == nullptr || q->items.empty()) return QueueStep::Idle;
    if (q->at >= q->items.size()) {
        stash_queue_clear(q);
        return QueueStep::Done;
    }
    if (!have_session) return QueueStep::NoSession;
    if (now < q->next_at) return QueueStep::Wait;
    return QueueStep::Send;
}

void stash_queue_sent(StashQueue* q, double now, double interval) {
    if (q == nullptr) return;
    if (q->at < q->items.size()) ++q->at;
    q->next_at = now + interval;
}

void stash_queue_clear(StashQueue* q) {
    if (q == nullptr) return;
    q->items.clear();
    q->at = 0;
    q->next_at = 0.0;
    q->no_session_noted = false;
}

std::size_t stash_queue_remaining(const StashQueue& q) {
    return q.at < q.items.size() ? q.items.size() - q.at : 0;
}

bool stash_autosave_due(double dirty_at, double now, double delay) {
    return dirty_at >= 0.0 && now - dirty_at >= delay;
}

}  // namespace cdtb::render
```

- [ ] **Step 4: 빌드·시험**

Run: `pwsh -NoProfile -File E:/CDToybox-sdd/scripts/build.ps1` 그리고 `E:/CDToybox-sdd/build/cdtb_tests.exe`
Expected: `445 tests, 0 failures` (438 + 7).

- [ ] **Step 5: 커밋**

```bash
git -C E:/CDToybox-sdd add CMakeLists.txt src/render/stash_queue.h src/render/stash_queue.cpp tests/stash_queue_tests.cpp
git -C E:/CDToybox-sdd commit -m "보관함 큐 상태 기계와 자동 저장 판정 (ImGui 없음, 시험 7개)" -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 자동 저장 · 이름 기반 담기 목적지 · `stash_tick` 골격

**Files:**
- Modify: `src/game/stash.h`(`remove_set` 선언 아래), `src/game/stash.cpp`(`remove_set` 정의 아래), `tests/stash_tests.cpp`(끝에 추가), `src/render/stash_panel.h`, `src/render/stash_panel.cpp`, `src/render/overlay.cpp`(`draw_windows`)

**Interfaces:**
- Consumes: Task 1 `stash_autosave_due`; 1단계 `Notice`(`render/notice.h`: `notice_set`, `notice_draw`, `NoticeLevel`).
- Produces: `int game::Stash::find_set(const std::string& name) const`; `void render::stash_tick()`; `stash_open_set()` 이 이름으로 해석(서명 불변); 내부 `mark_dirty()`, `save()` 가 `bool`.

- [ ] **Step 1: `find_set` 시험** — `tests/stash_tests.cpp` 끝에

```cpp
TEST(find_set_returns_the_index_of_the_named_set) {
    Stash s;
    s.add_set("무기");
    s.add_set("방어구");
    CHECK_EQ(s.find_set("방어구"), 1);
    CHECK_EQ(s.find_set("무기"), 0);
}

TEST(find_set_gives_minus_one_when_absent_or_empty) {
    Stash s;
    s.add_set("무기");
    CHECK_EQ(s.find_set("없는 세트"), -1);
    CHECK_EQ(s.find_set(""), -1);
}

// 세트를 지우면 옛 번호는 뜻이 없다 - 이름으로 다시 찾으면 -1 이거나 옮겨진 번호다.
TEST(find_set_follows_removal) {
    Stash s;
    s.add_set("무기");
    s.add_set("방어구");
    s.remove_set(0);
    CHECK_EQ(s.find_set("무기"), -1);
    CHECK_EQ(s.find_set("방어구"), 0);
}
```

- [ ] **Step 2: 빌드해 실패를 본다**

Run: `pwsh -NoProfile -File E:/CDToybox-sdd/scripts/build.ps1`
Expected: FAIL — `find_set` 은 `Stash` 의 멤버가 아니다.

- [ ] **Step 3: `find_set` 구현**

`src/game/stash.h` — `void remove_set(int i);` 다음 줄:

```cpp
    // 이름이 같은 첫 세트의 번호. 없거나 이름이 비면 -1. 세트가 지워지면 번호가
    // 밀리므로 "펼쳐 둔 세트" 는 번호가 아니라 이름으로 들고 여기로 푼다.
    int find_set(const std::string& name) const;
```

`src/game/stash.cpp` — `Stash::remove_set` 정의 다음:

```cpp
int Stash::find_set(const std::string& name) const {
    if (name.empty()) return -1;
    for (std::size_t i = 0; i < sets_.size(); ++i) {
        if (sets_[i].name == name) return static_cast<int>(i);
    }
    return -1;
}
```

Run: 빌드 + `cdtb_tests.exe` → `448 tests, 0 failures`.

- [ ] **Step 4: `stash_panel.h` 에 `stash_tick` 선언**

`void draw_stash_panel(bool* open);` 다음 줄에:

```cpp
// overlay::draw_windows() 가 매 프레임, 창을 그리기 전에 부른다. 창이 닫혀 있어도
// 자동 저장(변경 1초 뒤)과 일괄 지급 큐가 여기서 돈다. 창이 안 그려진 프레임에는
// "펼쳐 둔 세트" 를 비워 인벤 '보관' 이 닫힌 창의 세트에 담지 않게 한다.
void stash_tick();
```

- [ ] **Step 5: `stash_panel.cpp` — 상태·저장·목적지를 바꾼다**

(a) 포함: `#include "render/layout.h"` 다음에 `#include "render/notice.h"`, `#include "render/stash_queue.h"`, 그리고 `<ctime>` 을 `<cstring>` 다음에.

(b) 익명 네임스페이스의 상태 - 아래 넷을 이렇게 바꾼다:

```cpp
game::Stash g_stash;
bool g_loaded = false;
// 마지막 변경 시각(초). 음수면 저장할 것이 없다. 1초 뒤 stash_tick 이 저장한다 -
// 저장 버튼은 없다(★ 을 누르고 창을 안 열면 조용히 유실되던 것).
double g_dirty_at = -1.0;
char g_saved_clock[16] = "";        // 마지막 저장 HH:MM:SS. 비면 아직 없음
// 펼쳐 둔 세트. 번호로 들면 세트를 지운 뒤 엉뚱한 세트에 담긴다 - 이름으로 든다.
std::string g_open_set_name;
bool g_drawn_this_frame = false;    // draw_stash_panel 이 본문을 그렸는가
char g_new_name[64] = "";
Notice g_notice;
```

`g_dirty`·`g_open_set` 는 없앤다(전부 아래 헬퍼로 대체). `g_queue`·`g_queue_at` 은 이 태스크에서는 **그대로 둔다**(Task 3).

(c) 헬퍼 - `stash_path()` 앞에:

```cpp
void mark_dirty() { g_dirty_at = ImGui::GetTime(); }

void stamp_saved_clock() {
    const std::time_t t = std::time(nullptr);
    std::tm tm{};
    if (localtime_s(&tm, &t) == 0) {
        std::snprintf(g_saved_clock, sizeof(g_saved_clock), "%02d:%02d:%02d",
                      tm.tm_hour, tm.tm_min, tm.tm_sec);
    }
}
```

(d) `save()` 를 이렇게 바꾼다(반환 bool, 로그, 실패 Notice):

```cpp
bool save() {
    const std::wstring p = stash_path();
    if (p.empty()) return false;
    const std::string text = g_stash.serialize();
    HANDLE h = ::CreateFileW(p.c_str(), GENERIC_WRITE, 0, nullptr,
                             CREATE_ALWAYS, FILE_ATTRIBUTE_NORMAL, nullptr);
    if (h == INVALID_HANDLE_VALUE) {
        log::warnf("보관함을 저장하지 못했다 (열기 실패 {})", ::GetLastError());
        notice_set(&g_notice, NoticeLevel::Bad, "저장 실패 - 파일이 잠겼는지 보세요");
        // 1초마다 다시 실패하며 알림을 새로 찍지 않게 10초 뒤에 다시 본다.
        g_dirty_at = ImGui::GetTime() + 9.0;
        return false;
    }
    DWORD wrote = 0;
    const BOOL ok = ::WriteFile(h, text.data(), static_cast<DWORD>(text.size()),
                                &wrote, nullptr);
    ::CloseHandle(h);
    if (!ok || wrote != text.size()) {
        log::warnf("보관함을 저장하지 못했다 (쓰기 {}/{} 바이트)", wrote, text.size());
        notice_set(&g_notice, NoticeLevel::Bad, "저장 실패 - 파일이 잠겼는지 보세요");
        g_dirty_at = ImGui::GetTime() + 9.0;
        return false;
    }
    g_dirty_at = -1.0;
    stamp_saved_clock();
    log::infof("보관함 저장: 즐겨찾기 {}개, 세트 {}개", g_stash.favorites().size(),
               g_stash.set_count());
    return true;
}
```

(e) 공개 함수들:

```cpp
int stash_open_set() {
    if (!g_loaded) load();
    return g_stash.find_set(g_open_set_name);
}

const char* stash_open_set_name() {
    const int i = stash_open_set();
    if (i < 0) return "";
    const game::StashSet* s = g_stash.set_at(i);
    return s != nullptr ? s->name.c_str() : "";
}

bool stash_add_entry(int set, const game::StashEntry& entry) {
    if (!g_loaded) load();
    if (set < 0 || set >= g_stash.set_count()) return false;
    game::StashSet* s = g_stash.set_at(set);
    if (s == nullptr) return false;
    s->items.push_back(entry);
    mark_dirty();
    return true;
}

void stash_toggle_favorite(unsigned int key) {
    if (!g_loaded) load();
    g_stash.toggle_favorite(key);
    mark_dirty();
}

void stash_tick() {
    if (!g_loaded) load();
    const double now = ImGui::GetTime();
    if (stash_autosave_due(g_dirty_at, now)) save();
    // 지난 프레임에 창 본문을 안 그렸으면 펼쳐 둔 세트를 잊는다 - 닫힌 창의
    // 세트에 인벤 '보관' 이 담기지 않게. 한 프레임 늦는 것은 무해하다.
    if (!g_drawn_this_frame) g_open_set_name.clear();
    g_drawn_this_frame = false;
}
```

(f) `draw_stash_panel` 안:
- `begin_window(Win::Stash, open)` 이 true 인 직후에 `g_drawn_this_frame = true;`
- `ImGui::TextDisabled("게임 밖에 두는 목록입니다. ...")` 다음 줄에 `notice_draw(g_notice);`
- 즐겨찾기 `빼기`: `g_dirty = true;` → `mark_dirty();`
- `세트 만들기`: 
  ```cpp
  if (ImGui::Button("세트 만들기") && g_new_name[0] != 0) {
      g_stash.add_set(g_new_name);
      g_open_set_name = g_new_name;
      g_new_name[0] = 0;
      mark_dirty();
  }
  ```
- 세트 머리글이 열렸을 때 `g_open_set = i;` → `g_open_set_name = set->name;`
- `세트 지우기`·`지금 고른 아이템 담기`·항목 `빼기`·개수 변경의 `g_dirty = true;` → `mark_dirty();`
- 바닥(`ImGui::Separator();` 뒤)의 저장 버튼 블록 전체를 이것으로:
  ```cpp
  ImGui::TextDisabled("cdtoybox_stash.txt 에 자동 저장 · 마지막 %s",
                      g_saved_clock[0] != 0 ? g_saved_clock : "없음");
  if (g_dirty_at >= 0.0) {
      ImGui::SameLine();
      ImGui::TextColored(col::kBusy, "(저장 대기)");
  }
  ImGui::SameLine();
  if (ImGui::SmallButton("다시 읽기")) {
      g_stash = game::Stash{};
      g_open_set_name.clear();
      g_dirty_at = -1.0;
      load();
  }
  if (ImGui::IsItemHovered()) {
      ImGui::SetTooltip("파일로 되돌립니다 (아직 저장되지 않은 1초 안의 변경은 버립니다)");
  }
  ```

- [ ] **Step 6: `overlay.cpp` — 매 프레임 tick**

`draw_windows()` 의 첫 줄(`using cdtb::render::Win;` 다음)에:

```cpp
    // 창이 닫혀 있어도 보관함 자동 저장·일괄 지급 큐는 돈다.
    cdtb::render::stash_tick();
```

- [ ] **Step 7: 빌드·시험·grep**

Run: 빌드 + `cdtb_tests.exe` → `448 tests, 0 failures`. `grep -n "g_dirty\b\|g_open_set\b" E:/CDToybox-sdd/src/render/stash_panel.cpp` → 0줄.

- [ ] **Step 8: 커밋**

```bash
git -C E:/CDToybox-sdd add src/game/stash.h src/game/stash.cpp tests/stash_tests.cpp src/render/stash_panel.h src/render/stash_panel.cpp src/render/overlay.cpp
git -C E:/CDToybox-sdd commit -m "보관함: 변경 1초 뒤 자동 저장, 펼쳐 둔 세트를 이름으로, stash_tick 매 프레임" -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 일괄 지급 큐를 창 밖으로 · 본창 진행 줄 · 세션 없음 유지

**Files:**
- Modify: `src/render/stash_panel.h`, `src/render/stash_panel.cpp`, `src/render/overlay.cpp`(`draw_ui`)

**Interfaces:**
- Consumes: Task 1 `StashQueue`/`stash_queue_step`/`stash_queue_sent`/`stash_queue_clear`/`stash_queue_remaining`; Task 2 `stash_tick`, `g_notice`.
- Produces: `bool render::stash_queue_progress(std::size_t* done, std::size_t* total)`, `void render::stash_queue_cancel()`.

- [ ] **Step 1: 헤더** — `stash_tick();` 선언 다음에

```cpp
// 일괄 지급 진행. 돌고 있으면 true 와 보낸 개수/전체를 준다 - 본창이 보관함 창이
// 닫혀 있을 때 진행 줄을 그린다.
bool stash_queue_progress(std::size_t* done, std::size_t* total);
void stash_queue_cancel();
```

- [ ] **Step 2: `stash_panel.cpp` — 큐를 `StashQueue` 로**

(a) 상태: `std::vector<game::StashEntry> g_queue; std::size_t g_queue_at = 0;` 를

```cpp
StashQueue g_queue;
std::size_t g_queue_total = 0;   // 시작할 때의 개수. 진행 줄·완료 문구에 쓴다
```

(b) 기존 `draw_stash_panel` 맨 앞의 큐 블록(`if (g_queue_at < g_queue.size() && !game::spawn_pending(...)) { ... }`)을 **통째로 잘라** 아래 두 함수로 옮긴다(익명 네임스페이스, `save()` 다음). 인자 조립은 한 글자도 바꾸지 않는다.

```cpp
// 한 항목의 지급 인자. 상한 자르기·소켓 조립은 예전 큐 코드 그대로다.
game::GiveExtras extras_for(const game::StashEntry& e) {
    std::uint32_t cap = 0;
    if (const auto* c = game::item_by_key(e.key)) cap = c->max_temper;
    const std::uint32_t socket_cap = game::socket_room_for(e.key);

    game::GiveExtras extras;
    extras.temper = static_cast<std::uint16_t>(e.temper > cap ? cap : e.temper);
    const std::int16_t sharp_cap = game::max_sharpness_for(e.key);
    extras.sharpness = static_cast<std::uint16_t>(
        (sharp_cap > 0 && e.sharpness > static_cast<std::uint32_t>(sharp_cap))
            ? sharp_cap
            : e.sharpness);
    const std::uint16_t full = game::full_endurance_for(e.key);
    if (e.endurance == game::kStashNoEndurance) {
        extras.endurance = full;
    } else {
        extras.endurance =
            static_cast<std::uint16_t>(e.endurance > full ? full : e.endurance);
    }
    std::size_t room = socket_cap;
    if (room > game::kGiveMaxSockets) room = game::kGiveMaxSockets;
    for (const auto& sk : e.sockets) {
        if (extras.socket_count >= room) break;
        auto& dst = extras.sockets[extras.socket_count];
        if (!game::socket_bytes_for_key(sk.key, dst.raw)) {
            std::memcpy(dst.raw, sk.raw, game::kGiveSocketBytes);
        }
        ++extras.socket_count;
    }
    return extras;
}

// 큐를 한 발 진행한다. 창이 닫혀 있어도 stash_tick 이 부른다.
void run_queue(double now) {
    if (g_queue.items.empty()) return;
    // 앞 요청이 아직 게임 스레드에 있으면 이번 프레임은 쉰다.
    if (game::spawn_pending(game::DriveLane::Item)) return;
    const mem::LocalReader rd;
    const std::uintptr_t session = game::pick_drive_session(rd);
    switch (stash_queue_step(&g_queue, session != 0, now)) {
        case QueueStep::Idle:
        case QueueStep::Wait:
            return;
        case QueueStep::NoSession: {
            if (g_queue.no_session_noted) return;
            g_queue.no_session_noted = true;
            const std::size_t left = stash_queue_remaining(g_queue);
            notice_set(&g_notice, NoticeLevel::Warn,
                       "세션이 없어 {}개 남았습니다 - 월드에 들어가면 이어집니다", left);
            log::warnf("보관함 지급: 세션이 없어 {}개 남김 - 월드 진입 뒤 이어간다", left);
            return;
        }
        case QueueStep::Done:
            notice_set(&g_notice, NoticeLevel::Ok, "{}개 지급 완료", g_queue_total);
            log::infof("보관함 지급 완료: {}개", g_queue_total);
            return;
        case QueueStep::Send:
            break;
    }
    g_queue.no_session_noted = false;
    const game::StashEntry& e = g_queue.items[g_queue.at];
    if (game::request_give(session, e.key, e.count, extras_for(e))) {
        stash_queue_sent(&g_queue, now);
    }
    // false 면 레인 쿨다운 - 다음 프레임에 같은 항목을 다시 시도한다.
}

void queue_start(std::vector<game::StashEntry> items, const char* what) {
    stash_queue_clear(&g_queue);
    g_queue.items = std::move(items);
    g_queue_total = g_queue.items.size();
    log::infof("보관함 지급 시작: {} {}개", what, g_queue_total);
}
```

(c) `stash_tick()` 의 `if (stash_autosave_due(...)) save();` 다음 줄에 `run_queue(now);`

(d) 공개 함수(익명 네임스페이스 밖, `stash_tick` 다음):

```cpp
bool stash_queue_progress(std::size_t* done, std::size_t* total) {
    if (g_queue.items.empty()) return false;
    if (done != nullptr) *done = g_queue.at;
    if (total != nullptr) *total = g_queue.items.size();
    return true;
}

void stash_queue_cancel() {
    const std::size_t left = stash_queue_remaining(g_queue);
    stash_queue_clear(&g_queue);
    notice_set(&g_notice, NoticeLevel::Info, "중단했습니다 ({}개 남김)", left);
    log::infof("보관함 지급 중단: {}개 남김", left);
}
```

(e) `draw_stash_panel` 의 진행 줄:

```cpp
    std::size_t q_done = 0, q_total = 0;
    if (stash_queue_progress(&q_done, &q_total)) {
        ImGui::TextColored(col::kBusy, "지급 중 %zu / %zu (2초 간격)", q_done, q_total);
        ImGui::SameLine();
        if (ImGui::SmallButton("중단")) stash_queue_cancel();
    }
```

(f) 큐를 채우던 세 곳:
- 즐겨찾기 `지급`: `g_queue.assign(1, game::StashEntry{key, 1}); g_queue_at = 0;` → `queue_start({game::StashEntry{key, 1}}, "즐겨찾기");`
- `전부 지급`: `g_queue = set->items; g_queue_at = 0;` → `queue_start(set->items, set->name.c_str());`
- 세트 항목 `지급`: `g_queue.assign(1, set->items[j]); g_queue_at = 0;` → `set_grant_item_key(set->items[j].key); queue_start({set->items[j]}, "세트 항목");`

- [ ] **Step 3: 본창 진행 줄** — `overlay.cpp` `draw_ui()` 의 `ImGui::TextDisabled("* 게임 메모리를 바꾸는 창");` 다음에

```cpp
    // 보관함 창을 닫아도 일괄 지급은 stash_tick 이 이어 간다 - 진행을 여기서 보인다.
    std::size_t q_done = 0, q_total = 0;
    if (!shown(cdtb::render::Win::Stash) &&
        cdtb::render::stash_queue_progress(&q_done, &q_total)) {
        ImGui::TextColored(cdtb::render::col::kBusy, "보관함 지급 중 %zu / %zu",
                           q_done, q_total);
        ImGui::SameLine();
        if (ImGui::SmallButton("중단")) cdtb::render::stash_queue_cancel();
    }
```

- [ ] **Step 4: 빌드·시험·grep**

Run: 빌드 + `cdtb_tests.exe` → `448 tests, 0 failures`. `grep -n "g_queue_at\|g_queue\.clear\|g_queue\.assign\|g_queue = " E:/CDToybox-sdd/src/render/stash_panel.cpp` → 0줄. `draw_stash_panel` 안에 `request_give` 가 남아 있지 않다.

- [ ] **Step 5: 커밋**

```bash
git -C E:/CDToybox-sdd add src/render/stash_panel.h src/render/stash_panel.cpp src/render/overlay.cpp
git -C E:/CDToybox-sdd commit -m "보관함: 일괄 지급 큐를 창 밖으로 - 닫아도 이어지고 세션이 없으면 남긴다, 본창 진행 줄" -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 배치 · 확인 · 툴팁 · 로그 · 문서

**Files:**
- Modify: `src/render/stash_panel.cpp`, `docs/STATUS.md`(`tests/        438개` 줄, 6절 보관함 행)

**Interfaces:**
- Consumes: `confirm_small_button`(`render/confirm.h`), `set_grant_item_key`(`render/grant_panel.h`), `col::`.
- Produces: 없음(화면·문서).

- [ ] **Step 1: 이름 잘라 그리기 헬퍼** — 익명 네임스페이스 `draw_item_line` 앞에

```cpp
// 이름이 max_w 를 넘으면 글자(UTF-8) 경계에서 잘라 "…" 을 붙이고, 전체는 툴팁으로.
void draw_name_clipped(const char* name, ImVec4 color, float max_w) {
    const float w = ImGui::CalcTextSize(name).x;
    if (w <= max_w) {
        ImGui::TextColored(color, "%s", name);
        return;
    }
    std::string cut(name);
    while (!cut.empty() &&
           ImGui::CalcTextSize((cut + "…").c_str()).x > max_w) {
        cut.pop_back();
        while (!cut.empty() && (static_cast<unsigned char>(cut.back()) & 0xC0) == 0x80) {
            cut.pop_back();
        }
    }
    ImGui::TextColored(color, "%s…", cut.c_str());
    if (ImGui::IsItemHovered()) ImGui::SetTooltip("%s", name);
}
```

`draw_item_line(std::uint32_t key)` 을 `draw_item_line(std::uint32_t key, float name_w)` 으로 바꿔 이름 줄을 `draw_name_clipped(it->name.c_str(), grade_color(it->grade), name_w)` 로 그린다(`키 %u` 갈래는 그대로). 즐겨찾기 줄은 `ImGui::GetContentRegionAvail().x` 를, 세트 줄은 개수 칸 앞까지(`ImGui::GetContentRegionAvail().x - 150.0f`)를 넘긴다.

- [ ] **Step 2: 즐겨찾기 영역·머리글 개수**

즐겨찾기 블록을 이렇게:

```cpp
    const auto favs = g_stash.favorites();   // 지우면서 도니 복사한다
    char fav_hdr[48];
    std::snprintf(fav_hdr, sizeof(fav_hdr), "즐겨찾기 (%zu)###favs", favs.size());
    if (ImGui::CollapsingHeader(fav_hdr, ImGuiTreeNodeFlags_DefaultOpen)) {
        // 즐겨찾기가 많아도 세트를 스크롤 밖으로 밀어내지 않게 창 높이의 45% 까지만.
        const float row_h = kIconSize + ImGui::GetStyle().ItemSpacing.y;
        const float want = favs.empty() ? ImGui::GetTextLineHeightWithSpacing()
                                        : row_h * static_cast<float>(favs.size()) +
                                              ImGui::GetStyle().WindowPadding.y;
        const float cap = ImGui::GetWindowHeight() * 0.45f;
        ImGui::BeginChild("favs", ImVec2(0, want < cap ? want : cap), ImGuiChildFlags_None,
                          ImGuiWindowFlags_None);
        if (favs.empty()) {
            ImGui::TextDisabled("아이템 목록에서 별표를 눌러 담으세요");
        }
        for (const auto key : favs) {
            ImGui::PushID(static_cast<int>(key));
            if (ImGui::SmallButton("빼기")) {
                g_stash.toggle_favorite(key);
                log::infof("보관함: 즐겨찾기 {} 제거", key);
                mark_dirty();
            }
            ImGui::SameLine();
            if (ImGui::SmallButton("지급")) {
                set_grant_item_key(key);
                queue_start({game::StashEntry{key, 1}}, "즐겨찾기");
            }
            ImGui::SameLine();
            draw_item_line(key, ImGui::GetContentRegionAvail().x);
            ImGui::PopID();
        }
        ImGui::EndChild();
    }
```

세트 부분 `ImGui::Separator();` 다음에 머리글:

```cpp
    ImGui::Text("세트 (%d)", g_stash.set_count());
```

- [ ] **Step 3: 세트 만들기 비활성·지우기 확인·세트 줄 정렬·로그**

- `세트 만들기`:
  ```cpp
  ImGui::BeginDisabled(g_new_name[0] == 0);
  if (ImGui::Button("세트 만들기")) {
      g_stash.add_set(g_new_name);
      g_open_set_name = g_new_name;
      log::infof("보관함: 세트 '{}' 만듦", g_new_name);
      g_new_name[0] = 0;
      mark_dirty();
  }
  ImGui::EndDisabled();
  ```
- `세트 지우기`:
  ```cpp
  if (confirm_small_button("세트 지우기")) {
      log::infof("보관함: 세트 '{}' 지움 ({}개 항목)", set->name, set->items.size());
      g_stash.remove_set(i);
      mark_dirty();
      ImGui::PopID();
      break;
  }
  ```
  (`#include "render/confirm.h"` 추가.)
- 세트 항목 줄: `draw_item_line(set->items[j].key, ImGui::GetContentRegionAvail().x - 150.0f);` 다음, 개수 칸을 오른쪽에 고정:
  ```cpp
  // 오른쪽 끝에서 140px. GetWindowContentRegionMax 는 폐기 예정이라 커서 기준으로 잰다.
  ImGui::SameLine(ImGui::GetCursorPosX() + ImGui::GetContentRegionAvail().x - 140.0f);
  ```
  그 뒤의 `InputInt`(폭 90)·`/ N`·`x%lld` 는 그대로. `ImGui::SetNextItemWidth(110.0f)` → `90.0f`.
- `stash_toggle_favorite`(공개 함수) 에 로그: `toggle` 뒤 `log::infof("보관함: 즐겨찾기 {} {}", key, g_stash.is_favorite(key) ? "추가" : "제거");`

- [ ] **Step 4: 문서**

- `docs/STATUS.md`: `tests/        438개 (...)` → `448개`; 6절 표의 `| 보관함 | 즐겨찾기·세트, \`cdtoybox_stash.txt\` 에 저장 |` → `| 보관함 | 즐겨찾기·세트, \`cdtoybox_stash.txt\` 에 변경 1초 뒤 자동 저장. 일괄 지급 큐는 창을 닫아도 돌고 세션이 없으면 남는다(본창 진행 줄) |`.

- [ ] **Step 5: 빌드·시험**

Run: 빌드 + `cdtb_tests.exe` → `448 tests, 0 failures`.

- [ ] **Step 6: 커밋**

```bash
git -C E:/CDToybox-sdd add src/render/stash_panel.cpp docs/STATUS.md
git -C E:/CDToybox-sdd commit -m "보관함: 즐겨찾기 영역 상한·머리글 개수·세트 줄 정렬·지우기 확인·이름 잘림 툴팁·로그" -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 자체 검토 (플랜 작성 시)

- **스펙 5절 대조**: 자동 저장(`g_dirty_at`·`stash_tick`·버튼 제거·바닥 줄·실패 Bad·성공 로그) → Task 2. 큐 창 밖(`stash_queue.{h,cpp}`·`stash_tick`·본창 진행 줄+중단·NoSession 유지+Warn+warnf·Done Ok·시작/중단 로그) → Task 1+3. 담기 목적지 이름(`g_open_set_name`·`stash_open_set` 이름 해석·안 그린 프레임 비움) → Task 2. 즐겨찾기 영역 45%·머리글 개수 → Task 4. 세트 줄(`[빼기][지급] 아이콘 이름… [개수]/N` 오른쪽 고정·지우기 confirm·다시 읽기 툴팁·만들기 비활성·항목 지급 `set_grant_item_key`) → Task 2(툴팁)+3(항목 지급)+4(나머지). 로그(★·세트 추가/삭제·큐 시작/중단) → Task 3+4. 시험(`stash_queue_step`·`autosave_due`·이름 해석) → Task 1+2.
- **서명 편차**: `interval` 을 `stash_queue_step` 에서 `stash_queue_sent` 로 옮김(위 파일 구조 절).
- **자리표시자**: 없음. 모든 코드 단계에 코드가 있다.
- **형 일관성**: `stash_queue_progress(std::size_t*, std::size_t*)`·`stash_queue_cancel()` 은 Task 3 에서 정의하고 같은 태스크의 overlay.cpp 가 쓴다. `draw_item_line(key, name_w)` 서명 변경은 Task 4 안에서 정의·호출 둘 다 바꾼다(다른 파일은 안 부른다 - `grep draw_item_line src/render` 로 확인할 것).
- **6절 검증(3단계)**: 즐겨찾기 25개 상태에서 세트 머리글 / 창 닫고 큐 진행 줄(본창) / 자동 저장 시각 — 배포 뒤 컨트롤러가 `scripts/overlay-check.ps1` 로 본다(쓰기 버튼은 누르지 않는다; 큐 진행은 즐겨찾기 `지급` 한 건으로 본다 - 인벤토리 지급은 게임 메모리 쓰기가 아니라 게임의 지급 경로다).
