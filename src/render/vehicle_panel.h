#pragma once

// 드래곤 · A.T.A.G. 창.
//
// 예전에는 플레이어 치트 안의 한 절이었는데, 조사하면서 토글이 여덟까지
// 늘어 그 창에서 가장 큰 덩어리가 됐다. 성격도 다르다 - 플레이어 치트는
// **내 캐릭터**(생명·스태미나·낙사·스킬)이고 여기는 **동반자와 탈것**이다.
// 그래서 창을 갈랐다(2026-09-16).
//
// 가르면서 **실기능과 조사용을 나눠 그린다.** 드래곤 소환이 풀리기 전까지
// 실험으로 넣은 토글들이 그대로 남아 있어, 무엇을 켜야 하는지 화면만 봐서는
// 알 수 없었다. 지금 필요한 것은 위쪽 절들이고, 아래 "조사용" 은 평소에 끈다.

namespace cdtb::render {

// `open` 이 거짓이 되면(✕) 부르는 쪽이 다음 프레임부터 안 그린다.
void draw_vehicle_panel(bool* open);

}  // namespace cdtb::render
